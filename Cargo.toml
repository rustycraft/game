[package]
name = "game"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

winit = { version = "0.30", features = ["wayland"] }
wgpu = { version = "26.0" }
pollster = "0.4"
